/*Options from which can be chosen while negociating the sale of a savings account product

Options from whihc can be choosen while configuring the arrangemetns in an individual savings agreement. 

e.g. intrest calculation algorithms (modality), intrest calculation periods and intrestrate ranges (variable ranges); minimum deposit, max withdrawal amountrange (limit types and ranges*/
package org.museframework.bian.classes;

public class SavingsAccountProductFeatureOption {
    /*Indication wether a savings account feature option is default deof the default, i.e. valid if no specific arrangements are made*/
    private String defaultIndicator;

    public void setDefaultIndicator(String defaultIndicator) {
        this.defaultIndicator = defaultIndicator;
    }

    public String getDefaultIndicator() {
        return defaultIndicator;
    }
}